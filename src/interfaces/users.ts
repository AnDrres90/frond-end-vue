export interface Users {
    name: string;
    lastName: string;
    email: string;
    password: string;
    _id: string;
    createdAt: string;
    updatedAt: string;
    role: string;
}

export interface User {
    email: string;
    password: string;
}